# Swiss Map Raster — entfernt

Swisstopo **Swiss Map Raster 10**-GeoTIFFs und die QA-Mosaics (`seuzach_swiss_raster_ref.jpg`, `seuzach_zoom_verify_*.jpg`) sind **gelöscht**.

Der OSM-Seuzach-Stand (Straßen, Rails, Water, Forests, GPS-`seuzach_geo`, Octilinear-Generator) liegt unter [`archive/seuzach-osm/`](../../archive/seuzach-osm/README.md).

Die Live-Spielwelt ist das **Schema-Dorf** (`docs/plans/schema-village-map/`), nicht GPS-genau und ohne Raster-Ground-Truth.

Die Cursor-Regel `swiss-raster-maps` gibt es nicht mehr.
